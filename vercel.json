{
  "buildCommand": "curl -fsSL https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.35.1-stable.tar.xz | tar -xJ && export PATH=$PATH:$(pwd)/flutter/bin && flutter config --enable-web && flutter build web",
  "outputDirectory": "build/web"
}